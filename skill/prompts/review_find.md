Ты — суб-агент оркестратора, шаг **review:find** для фазы {N}.

## Контекст проекта (читай ВСЕГДА перед работой)

1. **`CLAUDE.md`** — продукт, аудитория.
2. **`idea/`** (если есть) — особенно `05-principles/`, `09-mvp/`. Нужно понять, что считается ценным поведением — то, что мы могли случайно сломать.
3. **`architecture/`** (если есть) — особенно `02-ядро/` (главный цикл), `03-данные/` (инварианты), `04-потоки/` (триггеры). Регрессии часто прячутся в нарушении этих инвариантов.
4. **`{PLAN_DIR}/promts/phase{N}.md`** — цель фазы.

## Что ещё прочитать для этого шага

5. **`{PLAN_DIR}/phase{N}/errors.md`** секцию `## APPLIED` — что фиксили.
6. **`{PLAN_DIR}/phase{N}/missing.md`** секцию `## APPLIED` — что добавили.
7. Текущий код после всех правок.

## Что искать — регрессии после errors+missing

- **Что появилось НОВОГО** в результате правок — новые функции, новые ветки выполнения, новые зависимости.
- **Что могли сломать** правками errors и missing — не возникли ли коллизии между фиксами, не сломал ли apply errors то, что добавил apply missing.
- **Архитектурные инварианты из `architecture/`** — могли ли мы случайно их нарушить, добавляя/правя код.

**Только новое.** Не повторяй то, что уже было в `{PLAN_DIR}/phase{N}/errors.md` или `{PLAN_DIR}/phase{N}/missing.md`. Если регрессия — это та же ошибка под другим именем, не записывай.

## Куда записать

Создай или дополни `{PLAN_DIR}/phase{N}/review.md`:

```markdown
## FOUND
1. <регрессия>: <в чём проблема; ссылка на нарушенный инвариант если применимо>
2. ...
```

Регрессий нет — запиши:

```markdown
## FOUND: пусто
```

В финальном ответе: `STATUS=done FILE={PLAN_DIR}/phase{N}/review.md FOUND=<число или "пусто">`.
