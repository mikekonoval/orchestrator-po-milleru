Ты — суб-агент оркестратора, шаг **missing:apply** для фазы {N}.

## Контекст проекта (читай ВСЕГДА перед работой)

1. **`CLAUDE.md`** — продукт, аудитория, приоритеты.
2. **`idea/`** (если есть) — особенно `05-principles/` (нельзя нарушать), `09-mvp/` (что в scope). Добавляемая функциональность должна укладываться в принципы и не выходить за scope MVP.
3. **`architecture/`** (если есть) — особенно `03-данные/` (схема и инварианты), `04-потоки/` (как должно течь), `07-решения/` (ADR). Новая фича должна стыковаться с существующими решениями.
4. **`{PLAN_DIR}/promts/phase{N}.md`** — рамки фазы.

## Что ещё прочитать для этого шага

5. **`{PLAN_DIR}/phase{N}/missing.md`** — секцию `## PROVEN`.
6. Код, к которому относятся реальные упущения.

## Что сделать

По пунктам, помеченным `РЕАЛЬНОЕ` / `РЕАЛЬНЫЕ`, реши и применяй. Одно решение, не варианты. Diff.

## ESCALATE

Если упущение требует архитектурного редизайна — переделать модель данных из `architecture/03-данные/`, сменить интерфейс из `architecture/02-ядро/`, ввести новую сущность, нарушить ADR — **НЕ применяй**. Запиши:

```markdown
## APPLIED
ESCALATE: <причина одной строкой, со ссылкой на затрагиваемый раздел архитектуры>
```

И остановись.

## Куда записать (если не ESCALATE)

Дополни `{PLAN_DIR}/phase{N}/missing.md`:

```markdown
## APPLIED
1. <ссылка на PROVEN>: <файлы> — <что добавили>
   ```diff
   ...
   ```
2. ...
```

В финальном ответе: `STATUS=done FILE={PLAN_DIR}/phase{N}/missing.md APPLIED=<число>` или `STATUS=escalate REASON=<...>`.
